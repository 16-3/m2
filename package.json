{
  "name": "scroll-server",
  "version": "1.0.0",
  "description": "A server that returns HTML source after scrolling to the bottom of the page.",
  "main": "app.js",
  "dependencies": {
    "express": "^4.17.1",
    "puppeteer": "^10.0.0"
  },
  "engines": {
    "node": ">=12.0.0"
  }
}
